{
  "name": "tailwind-v4-html-project",
  "version": "1.0.0",
  "description": "HTML project with Tailwind CSS v4",
  "main": "index.js",
  "scripts": {
    "build": "./build-styles.sh",
    "watch": "npm run build && echo 'Initial build complete. Manual re-run of npm run build is needed on changes.'"
  },
  "keywords": ["tailwind", "css", "html", "v4"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^4.0.0",
    "@tailwindcss/cli": "^4.0.0"
  }
}
